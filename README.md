# Correlations
A big data code that starts from preprocessing in search of correlations between features that lead to an expected result.
The features start from measurements in mm, microns, positions, tension force expressed in tons.
The final result is an algorithm that shows me the correlations, and which values need to be corrected for which the final result is the value 1(ok).
